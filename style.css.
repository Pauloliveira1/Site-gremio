/* Geral */
body {
    background-color: #D0E8FF; /* Fundo azul claro */
    font-family: 'Arial', sans-serif;
    color: white;
    margin: 0;
    padding: 0;
}

/* Cabeçalho */
header {
    background-color: #005F9F; /* Cor azul escuro para o cabeçalho */
    text-align: center;
    padding: 20px;
}

header .logo {
    width: 150px; /* Ajuste o tamanho da logo */
    height: auto;
}

header h1 {
    font-size: 2.5em;
    font-weight: bold;
    color: white;
    margin-top: 20px;
}

nav ul {
    list-style: none;
    padding: 0;
    margin-top: 20px;
}

nav ul li {
    display: inline;
    margin: 0 15px;
}

nav ul li a {
    text-decoration: none;
    color: white;
    font-weight: bold;
    font-size: 1.2em;
}

nav ul li a:hover {
    color: #FFD700; /* Cor amarela para hover */
}

/* Seções */
section {
    padding: 20px;
    margin: 20px;
    background-color: #007ACC; /* Azul mais forte para as seções */
    border-radius: 8px;
}

section h2 {
    font-size: 2em;
    font-weight: bold;
    color: white;
}

section p {
    font-size: 1.2em;
    color: white;
}

/* Link para o Instagram */
section a {
    color: #FFD700; /* Cor amarela para o link do Instagram */
    font-weight: bold;
    text-decoration: none;
}

section a:hover {
    text-decoration: underline;
}

/* Rodapé */
footer {
    background-color: #005F9F; /* Cor azul escuro para o rodapé */
    text-align: center;
    padding: 10px;
    color: white;
    position: fixed;
    width: 100%;
    bottom: 0;
}

/* Responsividade */
@media screen and (max-width: 768px) {
    header h1 {
        font-size: 2em;
    }

    nav ul li {
        display: block;
        margin: 10px 0;
    }
}
